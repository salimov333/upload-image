{
  "name": "upload-image",
  "version": "1.0.0",
  "description": "Upload and display an image in Browser using the FileReader object and the File API",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm dist -rf && mkdir dist && cp -r src/* dist",
    "publish": "gh-pages -d dist",
    "deploy": "npm run build & npm run publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/salimov333/upload-image.git"
  },
  "author": "salimov333",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/salimov333/upload-image/issues"
  },
  "homepage": "https://github.com/salimov333/upload-image#readme",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
